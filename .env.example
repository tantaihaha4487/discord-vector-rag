# Discord app
BOT_TOKEN=
CLIENT_ID=
GUILD_ID=

# Chat model
# Start with one provider key. Fallback providers are optional.
AI_PROVIDER_OPENROUTER_API_KEY=

# Optional: override fallback order. Providers without keys are skipped.
# AI_PROVIDERS=openrouter,nvidia,openai
